* { box-sizing: border-box; margin: 0; padding: 0; }

body { 
    background-color: #0d0c11; 
    color: #ffffff; 
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; 
    display: flex; 
    flex-direction: column; 
    align-items: center; 
    padding: 20px; 
    min-height: 100vh; 
    position: relative;
    overflow-x: hidden;
    background-image: 
        radial-gradient(circle at 10% 20%, rgba(186, 133, 219, 0.15) 0%, transparent 40%),
        radial-gradient(circle at 90% 80%, rgba(244, 172, 203, 0.15) 0%, transparent 40%);
    background-attachment: fixed;
    animation: auraPulse 8s ease-in-out infinite alternate;
}

/* --- COLAGEM ANIMADA NAS LATERAIS --- */
.side-banner {
    position: fixed;
    top: 0;
    width: calc((100vw - 650px) / 2);
    height: 100vh;
    z-index: -3;
    pointer-events: none;
    overflow: hidden;
}

.left-side {
    left: 0;
    /* Suaviza o encontro da imagem com a foto central */
    mask-image: linear-gradient(to right, rgba(0,0,0,0.6) 30%, rgba(0,0,0,0) 100%);
    -webkit-mask-image: linear-gradient(to right, rgba(0,0,0,0.6) 30%, rgba(0,0,0,0) 100%);
}

.right-side {
    right: 0;
    mask-image: linear-gradient(to left, rgba(0,0,0,0.6) 30%, rgba(0,0,0,0) 100%);
    -webkit-mask-image: linear-gradient(to left, rgba(0,0,0,0.6) 30%, rgba(0,0,0,0) 100%);
}

.collage-track {
    width: 100%;
    height: 200%;
    background-image: url('colagem_ari.jpg');
    background-size: cover;
    background-repeat: repeat-y;
    opacity: 0.45; /* Opacidade equilibrada para não poluir a leitura */
}

/* Animação do Lado Esquerdo (Subindo) */
.scroll-up {
    animation: sideScrollUp 35s linear infinite;
}

/* Animação do Lado Direito (Descendo) */
.scroll-down {
    animation: sideScrollDown 35s linear infinite;
}

@keyframes sideScrollUp {
    0% { transform: translateY(0); }
    100% { transform: translateY(-50%); }
}

@keyframes sideScrollDown {
    0% { transform: translateY(-50%); }
    100% { transform: translateY(0); }
}

/* Oculta as colagens em telas menores para celulares */
@media (max-width: 900px) {
    .side-banner {
        display: none;
    }
}

/* --- IMAGEM CENTRALIZADA ESTILO SWIFTLE --- */
body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100%;
    max-width: 650px;
    height: 100vh;
    background-image: linear-gradient(to bottom, #121212 0%, rgba(18, 18, 18, 0.6) 30%, rgba(18, 18, 18, 0.85) 80%, #121212 100%), url('ari_photo.jpg');
    background-size: cover;
    background-position: center bottom;
    background-repeat: no-repeat;
    z-index: -2;
    pointer-events: none;
}

/* --- ANIMAÇÃO DAS PÉTALAS --- */
.petals-container {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    margin: 0 auto;
    width: 100%;
    max-width: 650px;
    height: 100%;
    pointer-events: none;
    z-index: -1;
    overflow: hidden;
}

header { text-align: center; margin-bottom: 30px; }
h1 { color: #d8b4e2; font-size: 2.5rem; margin-bottom: 5px; }

.guesses-container { display: flex; flex-direction: column; gap: 8px; width: 100%; max-width: 400px; margin-bottom: 30px; }

/* --- CAIXAS DE PALPITE (DESTACADAS COM VIDRO FOSCO) --- */
.guess-box {
    background: rgba(20, 20, 25, 0.82);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 10px;
    color: #ffffff;
    font-weight: 700;
    font-size: 15px;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    padding: 0 15px;
    height: 42px;
    margin-bottom: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
    transition: all 0.3s ease;
}

.player-container { width: 100%; max-width: 400px; margin-bottom: 20px; }
.progress-bar-bg { background: #333; height: 12px; width: 100%; position: relative; margin-bottom: 15px; border-radius: 6px; overflow: hidden; }
.progress-bar-fill { background: #d8b4e2; height: 100%; width: 0%; transition: width 0.05s linear; }

.controls { display: flex; justify-content: center; }
#play-btn { 
    background: #d8b4e2; color: #121212; 
    border: none; padding: 12px 30px; 
    border-radius: 25px; font-weight: bold; font-size: 16px;
    cursor: pointer; transition: 0.2s;
}
#play-btn:hover { transform: scale(1.05); }

.search-container { display: flex; gap: 10px; width: 100%; max-width: 400px; flex-wrap: wrap; }
#search-input { flex: 1; min-width: 150px; padding: 12px; background: #222; border: 1px solid #444; color: #fff; border-radius: 4px; }
#submit-btn { padding: 12px 20px; background: #fff; color: #000; border: none; border-radius: 4px; cursor: pointer; font-weight: bold; }
#skip-btn { padding: 12px 20px; background: #444; color: #fff; border: none; border-radius: 4px; cursor: pointer; font-weight: bold; }

.input-wrapper { position: relative; flex: 1; min-width: 180px; }

.autocomplete-results {
    position: absolute;
    top: 100%; left: 0; right: 0;
    background: #222;
    border: 1px solid #444; border-top: none;
    max-height: 200px; overflow-y: auto;
    z-index: 10;
    border-bottom-left-radius: 4px; border-bottom-right-radius: 4px;
}

.autocomplete-item {
    padding: 10px 12px; cursor: pointer;
    border-bottom: 1px solid #333;
    transition: background 0.2s, color 0.2s;
}
.autocomplete-item:hover { background: #d8b4e2; color: #121212; font-weight: bold; }

.result-card {
    display: flex; flex-direction: column; align-items: center;
    background: #1e1e1e; padding: 25px; border-radius: 16px;
    border: 1px solid #333; width: 100%; text-align: center;
    animation: slideUp 0.5s ease-out;
}

@keyframes slideUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

.album-cover {
    width: 220px; height: 220px; border-radius: 12px; margin: 15px 0;
    box-shadow: 0 8px 20px rgba(216, 180, 226, 0.3);
    transform: scale(0);
    animation: popIn 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
    animation-delay: 0.2s; 
}

@keyframes popIn { to { transform: scale(1); } }

.result-title { font-size: 1.8rem; margin-bottom: 5px; color: #fff; }
.result-subtitle { color: #aaa; font-size: 0.95rem; margin-bottom: 10px; }
.win-text { color: #4CAF50; } 
.lose-text { color: #f44336; } 

.action-buttons-container {
    display: flex; gap: 12px; margin-top: 15px; width: 100%;
    justify-content: center; flex-wrap: wrap;
}

.action-btn {
    padding: 12px 24px; border: none; border-radius: 8px;
    font-weight: bold; font-size: 15px; cursor: pointer;
    transition: all 0.2s ease; display: flex; align-items: center; gap: 8px;
}

.btn-play-again { background: #d8b4e2; color: #121212; }
.btn-play-again:hover { transform: scale(1.05); background: #e5c8ee; }

.btn-copy { background: #4CAF50; color: #fff; }
.btn-copy:hover { transform: scale(1.05); background: #45a049; }

.difficulty-selector { margin-top: 15px; }
#difficulty {
    background: #1a1a1a; color: #d8b4e2; border: 1px solid #444;
    padding: 8px 16px; border-radius: 20px; font-size: 14px;
    font-weight: bold; cursor: pointer; outline: none; transition: all 0.2s ease;
}
#difficulty:hover, #difficulty:focus { border-color: #d8b4e2; background: #222; }

.petal {
    position: absolute;
    background: #ffb7c5; 
    border-radius: 15px 0 15px 0; 
    opacity: 0.3;
    animation: fall linear forwards;
}

@keyframes fall {
    0% { transform: translateY(-20px) rotate(0deg) scale(0.8); opacity: 0; }
    10% { opacity: 0.3; }
    100% { transform: translateY(105vh) rotate(360deg) scale(1.2); opacity: 0; }
}

.score-container {
    background: #222;
    border: 1px solid #444;
    padding: 8px 20px;
    border-radius: 20px;
    font-size: 16px;
    font-weight: bold;
    color: #fff;
    margin-top: 15px;
    display: inline-block;
}

#score-display {
    color: #4CAF50;
    font-size: 18px;
    margin-left: 5px;
}

@keyframes auraPulse {
    0% { background-size: 100% 100%, 100% 100%; }
    100% { background-size: 120% 120%, 120% 120%; }
}

.stars-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    pointer-events: none;
    z-index: -4;
    
    background-image: 
        radial-gradient(2px 2px at 5% 15%, #ffffff, rgba(0,0,0,0)),
        radial-gradient(2px 2px at 12% 45%, #e8b4f8, rgba(0,0,0,0)),
        radial-gradient(1.5px 1.5px at 18% 75%, #ffffff, rgba(0,0,0,0)),
        radial-gradient(2px 2px at 8% 90%, #f4b4cb, rgba(0,0,0,0)),
        radial-gradient(2px 2px at 85% 10%, #e8b4f8, rgba(0,0,0,0)),
        radial-gradient(1.5px 1.5px at 92% 35%, #ffffff, rgba(0,0,0,0)),
        radial-gradient(2.5px 2.5px at 88% 68%, #f4b4cb, rgba(0,0,0,0)),
        radial-gradient(2px 2px at 95% 85%, #ffffff, rgba(0,0,0,0));
    background-repeat: repeat;
    animation: starsTwinkle 3s ease-in-out infinite alternate;
}

@keyframes starsTwinkle {
    0% { opacity: 0.3; }
    100% { opacity: 0.9; }
}

/* --- CONTAINER CENTRAL DO JOGO (RETÂNGULO) --- */
.game-wrapper {
    width: 100%;
    max-width: 620px; /* Pouco menor que a imagem de fundo para a borda aparecer bem */
    margin-top: 10px;
    padding: 40px 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    
    /* Estilo da Borda e Vidro Fosco (Glassmorphism) */
    border: 1px solid rgba(216, 180, 226, 0.3); /* Borda lilás suave */
    border-radius: 24px; /* Cantos arredondados */
    background: rgba(18, 18, 18, 0.4); /* Fundo escuro semi-transparente */
    backdrop-filter: blur(4px); /* Desfoque sutil do fundo */
    -webkit-backdrop-filter: blur(4px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5); /* Sombra para dar profundidade */
    
    z-index: 1; /* Garante que fique acima dos fundos */
}
/* --- MODAL DE REGRAS --- */
.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.75);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
    padding: 20px;
    animation: fadeIn 0.3s ease;
}

.modal-content {
    background: rgba(22, 20, 28, 0.95);
    border: 1px solid rgba(216, 180, 226, 0.4);
    border-radius: 20px;
    padding: 30px 25px;
    max-width: 440px;
    width: 100%;
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.6);
    position: relative;
    text-align: center;
    animation: modalPop 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.close-btn {
    position: absolute;
    top: 15px;
    right: 18px;
    background: none;
    border: none;
    color: #aaa;
    font-size: 24px;
    cursor: pointer;
    transition: color 0.2s;
}
.close-btn:hover { color: #fff; }

.modal-content h2 {
    color: #d8b4e2;
    font-size: 1.8rem;
    margin-bottom: 4px;
}

.modal-subtitle {
    color: #888;
    font-size: 0.9rem;
    margin-bottom: 20px;
}

.rules-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
    text-align: left;
    margin-bottom: 25px;
}

.rule-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    background: rgba(255, 255, 255, 0.04);
    padding: 10px 14px;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.06);
}

.rule-icon {
    font-size: 1.3rem;
    line-height: 1.2;
}

.rule-item strong {
    display: block;
    color: #fff;
    font-size: 0.95rem;
    margin-bottom: 2px;
}

.rule-item p {
    color: #bbb;
    font-size: 0.85rem;
    margin: 0;
    line-height: 1.3;
}

.btn-start-game {
    width: 100%;
    padding: 14px;
    background: linear-gradient(135deg, #d8b4e2 0%, #b88ad3 100%);
    color: #121212;
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(216, 180, 226, 0.3);
    transition: transform 0.2s, box-shadow 0.2s;
}

.btn-start-game:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(216, 180, 226, 0.5);
}

/* BOTÃO DE AJUDA NO HEADER */
.header-top {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
}

.help-btn {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #fff;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    cursor: pointer;
    font-size: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
}

.help-btn:hover {
    background: #d8b4e2;
    color: #121212;
    transform: scale(1.1);
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes modalPop {
    from { opacity: 0; transform: scale(0.85); }
    to { opacity: 1; transform: scale(1); }
}